version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: ecommerce-sql
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=M12*117Go
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    restart: unless-stopped